<Window x:Class="ExecuteSqlREquest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExecuteSqlREquest"
        mc:Ignorable="d"
        Title="PostgreSQL Query Executor" Height="450" Width="800">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Zone de saisie -->
    <TextBox x:Name="QueryTextBox" Height="100" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />

    <!-- Bouton et ProgressBar -->
    <Grid Grid.Row="1" Margin="0,10,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Button Name="ExecuteButton" Content="Exécuter la requête" Height="50" Click="ExecuteQuery_Click" HorizontalAlignment="Stretch"/>
      <ProgressBar x:Name="QueryProgressBar" Grid.Column="1" Width="200" Margin="10,0,0,0" Height="20" Visibility="Collapsed" />
    </Grid>

    <!-- Affichage des résultats -->
    <DataGrid x:Name="ResultDataGrid" Grid.Row="2" Margin="0,10,0,0" />
  </Grid>
</Window>
